# 签到页面使用说明

## 功能概述

这是一个基于Vue3+UniApp开发的签到页面模板，完全按照您提供的设计图实现。页面包含以下功能：

- 🎯 每日签到奖励系统
- 💰 积分累计功能
- 📱 响应式设计，适配不同屏幕尺寸
- 🎨 精美的UI设计，包含渐变背景和动画效果
- 💾 本地数据持久化存储

## 文件结构

```
src/
├── pages/
│   └── signin/
│       └── index.vue          # 签到页面主组件
├── store/
│   └── signin.ts              # 签到状态管理（Pinia）
└── pages.json                 # 页面路由配置
```

## 主要功能

### 1. 签到奖励系统
- 7天循环签到奖励
- 第1-6天：获得对应天数的积分（1-6积分）
- 第7天：获得7积分，并有特殊的钱袋图标
- 连续签到天数累计显示

### 2. 状态管理
使用Pinia进行状态管理，包含：
- 用户积分
- 签到状态
- 累计签到天数
- 签到天数配置

### 3. 数据持久化
- 使用uni.setStorageSync进行本地存储
- 自动检测日期变化，重置每日签到状态
- 数据在应用重启后保持

## 使用方法

### 1. 页面跳转
从首页点击"每日签到"按钮即可跳转到签到页面：

```javascript
uni.navigateTo({
    url: '/pages/signin/index'
})
```

### 2. 状态管理使用
```javascript
import { useSigninStore } from '@/store/signin'

const signinStore = useSigninStore()

// 执行签到
const result = await signinStore.signin()

// 获取用户积分
const userPoints = signinStore.userPoints

// 检查是否已签到
const isSignedToday = signinStore.isSignedToday
```

## 样式特点

### 1. 设计风格
- 粉色到橙色的渐变背景
- 白色卡片式主内容区域
- 绿色签到按钮
- 响应式网格布局

### 2. 动画效果
- 钱袋图标的弹跳动画
- 按钮点击反馈
- 签到状态切换动画

### 3. 响应式设计
- 支持不同屏幕尺寸
- 小屏幕自动调整网格列数
- 适配各种设备

## 自定义配置

### 1. 修改奖励配置
在 `src/store/signin.ts` 中修改签到天数配置：

```typescript
signinDays: [
  { day: 1, points: 1, completed: false, isCurrent: false },
  { day: 2, points: 2, completed: false, isCurrent: false },
  // ... 更多天数配置
]
```

### 2. 修改样式主题
在 `src/pages/signin/index.vue` 的样式部分修改颜色和布局：

```scss
.signin-container {
  background: linear-gradient(180deg, #ff6b9d 0%, #ff8a80 50%, #ffb74d 100%);
}
```

### 3. 添加更多功能
- 签到历史记录
- 连续签到奖励
- 积分兑换功能
- 签到提醒功能

## 技术栈

- **框架**: Vue 3 + Composition API
- **状态管理**: Pinia
- **UI组件**: uview-plus
- **样式**: SCSS
- **平台**: UniApp (支持微信小程序、H5、App等)

## 注意事项

1. 确保项目中已安装并配置了Pinia
2. 需要uview-plus组件库支持
3. 本地存储功能需要在小程序或App环境中测试
4. 建议在真机上测试动画效果

## 扩展建议

1. **后端集成**: 将签到数据同步到服务器
2. **推送通知**: 添加签到提醒功能
3. **社交功能**: 分享签到成就
4. **更多奖励**: 添加实物奖励或优惠券
5. **数据分析**: 统计用户签到行为

这个签到页面模板完全按照您的设计图实现，具有良好的用户体验和可扩展性。您可以根据具体需求进行进一步的定制和功能扩展。
